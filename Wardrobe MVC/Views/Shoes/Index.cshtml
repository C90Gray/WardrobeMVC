@model IEnumerable<Wardrobe_MVC.Models.Sho>

@{
    ViewBag.Title = "Index";
}

<h2 class="text-center">Shoes</h2>

<p class="text-center">
    <a class="btn btn-default" href="~/Shoes/Create">Create New &raquo;</a>
</p>
<div class="row text-center">
    @foreach (var item in Model)
    {
        <div class="col-lg-3 col-md-4 col-sm-6">
            <div class="img-thumbnail">
                <div class="caption">
                    <h5>
                        @Html.DisplayFor(modelItem => item.ShoeName)
                    </h5>
                    <p>
                        <img class="indexpic" src="@Html.DisplayFor(modelItem => item.ShoePic)" />
                    </p>
                    <p>
                        @Html.ActionLink("Edit", "Edit", new { id = item.ShoeID }) |
                        @Html.ActionLink("Details", "Details", new { id = item.ShoeID }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.ShoeID })
                    </p>
                </div>
            </div>
        </div>
    }
</div>